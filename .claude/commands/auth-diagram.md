Find all authentication-related code in this project (login, logout, session management, token handling, guards, middleware, route protection, etc.) and draw a Mermaid flowchart diagram showing the full authentication flow.

Include:
- Entry points (login page, protected routes)
- Auth checks and decision branches
- Token/session creation and validation
- Redirect logic
- Error/failure paths

Output the diagram as a fenced ```mermaid code block with a brief explanation of each step.

If no authentication exists yet, analyze the current app flow and describe what auth flow would need to be added to protect the relevant screens.
